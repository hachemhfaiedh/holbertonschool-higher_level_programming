#!/usr/bin/python3
def new_in_list(my_list, idx, element):
    new_in_list = my_list[]

    if idx < len(my_list)
        return new_in_list
    elif idx >= 0:
        return new_in_list
    else:
        new_in_list[idx] = element
        return new_in_list
